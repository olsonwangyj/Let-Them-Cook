# Week 7 teacher demonstration — 3–5 minutes

Before speaking, complete the [operator checklist](README.md). Open [the brief](teacher-brief.html) or [printable PDF](teacher-brief.pdf), [packet walkthrough](packet-walkthrough.md), the [long-run iPhone audit](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone6100-agent-20260908-225451/supplemental-audit.json) and the saved desktop 600-second evidence. The pack also carries [a recorded 100-packet desktop example](recorded-demo100.jsonl) with [provenance and hashes](evidence-index.json); keep its desktop label when presenting it. Keep password and serial-passkey terminals off the shared screen.

## Talk track

**0:00–0:35 — State what is being demonstrated.**

“Week 7 tests the communication chain using predictable dummy data. This ESP32 sends a fixed packet over protected BLE to the Laptop. The Laptop sends it to the actual Ultra96, which creates a deterministic result for an independent viewer. The values and gesture labels are test data; we are demonstrating transport and correlation, not sensor accuracy or AI inference.”

“The replacement iPhone's latest 100-result regression has exact matching IDs, zero receiver reconnects and zero checked sender error, drop, gap and callback counters. A separate 6,100-result run has exact IDs and zero receiver reconnects, with one retained sender callback discard. We also verified local-forward restoration and restarted the Python receiver. Physical foreground/lifecycle confirmation and teammate visualizer integration remain separate work.”

If showing saved files, say “recorded iPhone run.” If running the desktop rehearsal live, name that endpoint explicitly; the recorded iPhone result does not change the live topology.

**0:35–1:15 — Point along the connections.**

“The Laptop's ingestion path and the viewer's result path use separate SSH processes through `stujump.comp.nus.edu.sg`. Ultra96 exposes only SSH on TCP 22. Ingestion port 8888 and result port 9999 are loopback services on the board; the clients reach them through local forwarding. SSH checks both host keys, and the inner TLS connection checks our CA and the service name `ultra96.week7.internal`. The Laptop's bridge receives acknowledgements; results use the independent viewer connection.”

Point to the verified current server source/loopback bindings and the two current tunnel owners. A local port listing alone does not prove execution on the Ultra96.

**1:15–2:00 — Explain one packet.**

“Each BLE notification is exactly 32 bytes at approximately 10 Hz. It contains the `W7` marker, version, device ID, boot ID, sequence number, uptime and eight signed dummy values. For sequence 42, the values are `[-958,-948,-938,-928,-918,-908,-898,-888]`. The Laptop decodes those bytes and sends a length-prefixed `SENSOR_BATCH` JSON frame. Ultra96 validates it, acknowledges acceptance and emits `OPEN`, selected by sequence modulo four.”

Use [the packet example](packet-example.json): “The example trace `1:7:42` means device 1, boot 7, sequence 42. This is an explanatory fixture. In the live run, we inspect the actual boot and sequence IDs.” Confidence `1.0` is a dummy constant, not measured model confidence. BLE's packet fields are little-endian; the network JSON length prefix is four-byte big-endian.

**2:00–3:05 — Run and inspect the live 100.**

Start the prepared protected BLE command from the checklist. Show `subscribed`, a live ACK and its matching result ID. In actual Phone mode, show the Phone's readiness and display and start only the logging bridge sender.

“The ACK tells us that the board accepted the input. The result tells us that the independent viewer received an output. They can arrive in either order because they travel on separate streams, so we compare the complete ID sets. We require 100 unique matches, with no missing, unexpected or repeated result IDs.”

Show the sender summary, exact-ID audit and original Phone status together. The latest regression IDs are `1:2375739948:6503` through `:6602`; sender duration is 14.078 seconds, receiver/sender exits are 0, and the Phone summary reports `received=100,reconnects=0`. All checked sender error/drop/gap/callback counters are zero. The earlier replacement-Phone short run, IDs `:201` through `:300`, retained one callback-generation discard and remains separately recorded. The [earlier original Phone log](D:/LetThemCook-builds/iphone-live-20260907/phone100-20260908-185338/phone100.DDMJjm/phone100.combined.log) remains unchanged: IDs `:0` through `:99`, one startup `TimeoutError`, `received=100,reconnects=1`, sender duration 14.078 seconds. Keep run identities distinct. For a fresh run, report its actual outcome and retain any failure.

**3:05–3:50 — Show sustained evidence.**

“The separate long actual iPhone capture contains 6,100 ordered unique results, IDs `1:2375739948:302` through `:6401`, exactly matching packet records, sender ACKs and board acceptance. All six saved exits are zero. Source uptime spans 609.900 seconds and ACK activity 609.781 seconds, with a maximum ACK gap of 0.360 seconds. The sender reports 616.704 seconds elapsed. The Phone reports zero reconnects. One sender callback-generation discard makes the strict zero-drop check fail; exact-ID correlation still passes.”

The Phone log has no per-result timestamps. Full-run physical foreground confirmation remains pending. The console helper wrote 3,393 lines during the latter 339.476 seconds; that does not prove screen visibility or continuous Phone delivery. The local orchestrator's 618.250-second elapsed time is another distinct measurement.

“We also ran the protected ESP-to-Ultra96 path with an independent desktop viewer for 600.563 seconds. It produced 5,965 accepted ACKs and 5,965 exactly matching results, exceeding the 5,400 minimum. Both client streams and the Ultra96 acceptance log agree. There was one BLE connection, one ingestion connection and one viewer connection, with no reboot or checked stream/transport errors. One late callback was discarded during shutdown and remains recorded.”

Show the saved soak's audit, not a claim that the current short presentation has run for ten minutes. Maximum recorded ACK/result gaps were 0.375/0.500 seconds; these are stream activity gaps, not synchronized end-to-end latency measurements.

**3:50–5:00 — Show controlled recovery and state the next gate.**

“On the actual Phone, we removed only the application local forward, verified the listener was absent and observed connection refusal. We restored the forward, verified TLS, observed re-subscription and received 100 exactly matched results with one expected reconnect. We then restarted the Python receiver and ran the latest 100-result regression. This is controlled listener restoration before production; SSH-master/VPN loss, active-channel interruption and iOS app/screen actions remain untested.”

If time allows, show the historical hardware recovery separately: “Separate live captures now include USB-only power loss and the physical RESET button. USB recovery yielded 2,715 matching ACKs/results over 300.422 seconds; RESET yielded 5,832 over 600.422 seconds. Each showed a new boot and authenticated BLE recovery. Both remain `passed=false` because the interruptions broke the clean-run criteria. A separate desktop check passed with 100 matches in 13.797 seconds and zero checked errors or drops. We independently matched the measured IDs to the Ultra96 acceptance log.”

“The actual iPhone has now recorded 100 and 6,100 matching results with zero receiver reconnects; the longer capture covers more than ten minutes of source and ACK activity. Full-run physical foreground visibility, Phone timing, SSH-master/VPN loss, physical app/screen lifecycle and the teammate's visualizer remain unverified. The portable C# core has been compiled and tested; the Unity component and actual app still need their target environment. Actual Phone TLS accepted the trusted identity and rejected the wrong name and an untrusted CA.”

Show the separate USB, RESET and final-clean records below. The observed serial and stream gaps do not measure the exact physical action duration or synchronized end-to-end latency. Recovery does not imply lossless delivery across either interruption.

## Evidence to have open

| Claim | Source to show | Exact scope |
|---|---|---|
| Fixed packet, framing and deterministic output | [Selected design](../week7-selected-design-2026-09-06.md), [packet walkthrough](packet-walkthrough.md), [codec](../../common/sensor.py) | W7 v1, 32 bytes, eight dimensionless int16 values, 10 Hz; no real sensor/AI claim. |
| Actual board and independent SSH route | [Latest report: board and deployment](../week7-continuation-report-2026-09-07.md#actual-board-and-deployment), current day-of identity checks | Real Ultra96 `source-db6769a`; only SSH TCP 22 is externally reachable. PID 43932 was the last recorded owner and must be revalidated. |
| Latest replacement-iPhone regression after recovery | [Original Phone log](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/post-recovery-phone100-agent-20260908-231307/phone100.combined.log), [supplemental audit](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/post-recovery-phone100-agent-20260908-231307/supplemental-audit.json), [evidence index](evidence-index.json) | 100 exact Packet/ACK/Phone/board IDs `1:2375739948:6503` through `:6602`; zero Phone reconnects and all checked sender error/drop/gap/callback counters zero. Sender 14.078 s; source/ACK spans 9.900/9.750 s. Sender/receiver exits 0; capture exit 0 in command output, separate capture/wrapper exit files absent. Original log SHA-256 `216db268e40b058faa5b7cf79415cf46f5594154547cf8890295b8096b328169`. |
| Controlled Phone local-forward restoration and receiver restart | [Fault audit](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/forward-recovery-agent-20260908-230910/fault-audit.json), [original receiver log](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/forward-recovery-agent-20260908-230910/phone-original/receiver.combined.log), [evidence index](evidence-index.json) | Listener absence and connection refusal observed; TLS restored, re-subscription and 100 exact IDs `1:2375739948:6403` through `:6502`. One expected Phone reconnect; checked sender counters zero. Fresh receiver execution verified. No active-channel, SSH-master/VPN or physical app/screen interruption claim. |
| Earlier replacement iPhone 100-result rerun | [Original Phone log](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone100-OMIcKh/phone100.combined.log), [supplemental audit](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone100-OMIcKh/supplemental-audit.json), [board snapshot](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone100-OMIcKh/phone100-OMIcKh.server.log) | 100 ordered IDs `1:2375739948:201` through `:300`; exact Packet/ACK/Phone/board match. Zero Phone reconnects; saved sender/receiver exits 0. Sender 13.578 s; one callback-generation discard. Original log SHA-256 `26db4a18b395e39ce567ccb9ccc8dd42cf5e5ae7e79af4a5f54bf24ea15fff50`. |
| Replacement iPhone 6,100-result capture | [Original Phone log](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone6100-agent-20260908-225451/phone6100.combined.log), [supplemental audit](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone6100-agent-20260908-225451/supplemental-audit.json), [board snapshot](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/evidence/phone6100-agent-20260908-225451/phone6100-OPiFjB.server.log) | 6,100 exact ordered IDs `1:2375739948:302` through `:6401`; zero Phone reconnects; six saved exits 0. Source/ACK spans 609.900/609.781 s; max ACK gap 0.360 s; sender elapsed 616.704 s. One callback discard is the sole strict zero-drop failure. Physical foreground confirmation and Phone timing unverified. Original log SHA-256 `2421f7706a807cf6bfe485d817ce7df3b1f6fcfb08bca2dfab6b1c0d096455ea`. |
| Actual replacement-Phone trust | [TLS/security output](D:/LetThemCook-builds/iphone-control-20260908/w7-fd3c60de/phone-tls-security-check-after-compat.stdout.json) | TLS 1.3 accepted with trusted identity; wrong-name code 62 and untrusted-CA code 20 rejected. Management commands inside iSH do not establish iOS UI control. |
| Earlier actual iPhone 100-message delivery | [Original combined Phone log](D:/LetThemCook-builds/iphone-live-20260907/phone100-20260908-185338/phone100.DDMJjm/phone100.combined.log), [full Phone audit](D:/LetThemCook-builds/iphone-live-20260907/phone100-20260908-185338/phone-full-audit.json) | 100 ordered results `1:2375739948:0` through `:99`, matching all sender ACKs and board acceptance records. One `TimeoutError` before first result; no further logged reconnect; receiver exit 0. Sender duration 14.078 s. Original 16,727-byte log SHA-256: `72bcbcc14bd542816258dd04625e2438a3a99eac0736de536abdf73e33c4c1a8`. No zero-reconnect, Phone-duration or full Gate M claim. |
| Portable recorded 100-packet example | [Included JSONL](recorded-demo100.jsonl), [provenance and hashes](evidence-index.json) | 100/100 in 14.110 s, traces `1:738264655:0` through `:99`. Saved actual ESP/Ultra96/desktop evidence; offline audit is not a new physical run. |
| Clean protected 600-second desktop route | [600-second JSONL](D:/LetThemCook-builds/remote-evidence-20260907/remote-protected600.jsonl), [independent audit](D:/LetThemCook-builds/remote-evidence-20260907/independent-evidence-audit.json), [report](../week7-continuation-report-2026-09-07.md#actual-remote-acceptance-evidence) | 5,965/5,965 in 600.563 s; actual protected BLE and Ultra96, independent desktop subscriber. No Phone claim. |
| Observed USB-only power loss and recovery | [USB capture](D:/LetThemCook-builds/physical-live-20260907/usb-and-reset.jsonl), [safe serial record](D:/LetThemCook-builds/physical-live-20260907/usb-and-reset-serial.jsonl), [independent USB audit](D:/LetThemCook-builds/physical-live-20260907/independent-usb-audit.json) | 2,715 exact ACK/results in 300.422 s; boot `4133030820` → `1986177356`. Operator confirmed USB was the only power source. Serial error-to-reopen interval 21.431 s; maximum ACK/result gaps 25.656/25.594 s. Recovery reported authentication mode 13 and MTU 517. This file captured USB only despite its original filename; fault remains `passed=false`. |
| Observed physical RESET with USB connected | [RESET capture](D:/LetThemCook-builds/physical-live-20260907/reset-button-extended.jsonl), [safe serial record](D:/LetThemCook-builds/physical-live-20260907/reset-button-extended-serial.jsonl), [independent RESET audit](D:/LetThemCook-builds/physical-live-20260907/independent-reset-audit.json) | 5,832 exact ACK/results in 600.422 s: 4,526 from boot `1986177356`, sequences 3345–7870, and 1,306 from boot `1594988637`, sequences 1–1306. Serial handle stayed open with no serial errors; recovery reported authentication mode 13 and MTU 517. Maximum ACK/result gaps 14.000/13.938 s; fault remains `passed=false`. |
| Clean regression after both physical recoveries | [Final 100-packet capture](D:/LetThemCook-builds/physical-live-20260907/final-post-reset100.jsonl), [independent audit including final regression](D:/LetThemCook-builds/physical-live-20260907/independent-reset-audit.json), [report with board acceptance checks](../week7-continuation-report-2026-09-07.md) | 100 exact matches in 13.797 s; boot `1594988637`, sequences 1308–1407. `passed=true`; all checked stream errors, drops and shutdown callback drops zero; maximum ACK/result gap including trailing silence 0.250 s. USB, RESET and final-clean measured ranges independently matched the actual board's acceptance log. The earlier portable recorded example retains its original provenance. |
| Fault recovery and security negatives | [Recorded fault and live protocol checks](../week7-continuation-report-2026-09-07.md#deliberately-induced-faults), [protected firmware evidence](../week7-packet-firmware-evidence.md) | Intentional faults remain `passed=false`; wrong CA/SAN and invalid input rejected. No durable exactly-once or lossless-outage claim. |
| Remaining Phone and visualizer requirements | [Phone guide](../week7-phone-runbook.md), [iPhone quickstart](../week7-iphone-quickstart.md), [original goal](../week7-development-plan.md), [current acceptance table](../week7-continuation-report-2026-09-07.md#acceptance-status-and-remaining-work) | The latest 100-result regression has zero Phone reconnects and checked sender counters. The earlier 100- and 6,100-result captures retain one sender callback discard each. Local-forward restoration and receiver restart are verified. The long source/ACK spans exceed 600 s; full-run visible foreground confirmation, Phone timing, SSH-master/VPN loss, physical app/screen lifecycle, teammate visualizer and full Gate M remain unverified. Portable C# core tested; supplied Unity component uncompiled here. The updated Python receiver ran on actual Phone, without proving the historical timeout cause. |

## Likely questions

**Is the data fake?** The values are intentionally deterministic. With `--ble`, they originate on the physical ESP and cross real protected BLE. A synthetic Laptop run is explicitly labelled; it tests the network path without proving BLE.

**Why does the Phone need a tunnel?** Only Ultra96 SSH port 22 is externally accessible. The Phone's own SSH process forwards its local 19999 to Ultra96 loopback 9999, independently of the Laptop's ingestion connection.

**How do you know the received result belongs to this input?** The trace is `device_id:boot_id:seq`. We compare complete accepted-ACK and result sets, and independently compare the board's acceptance log. Equal totals alone are insufficient.

**Does a Phone log prove uninterrupted delivery and no duplicates?** The receiver saves results after duplicate suppression and includes no per-result timestamps. The audit establishes exact saved-ID correlation. A 600-second Phone continuity claim needs a separately timed on-device display/status observation; zero duplicate saved rows cannot establish zero duplicate wire arrivals. The sender's five-second ACK-silence check covers ingestion, not Phone result timing.

**What does the startup timeout mean?** The original full log shows one read timeout after subscription and before the first result, then 100 matching results with no further logged reconnect. It proves a retry occurred, not the precise upstream cause. The derived result-only JSONL omits status lines, so show it together with the original log and full audit. The updated receiver subsequently ran on the replacement Phone with zero reported reconnects. This does not independently identify the earlier timeout cause or prove that a deliberate wait beyond five seconds was exercised.

**What happens when a connection drops?** Reconnects use bounded backoff. Queues are bounded and stale data is dropped. The viewer receives fresh live results after re-subscription; disconnected results are not replayed. Loss and ambiguous sends stay visible in the counters.

**Is this secure or production ready?** The selected demo uses authenticated BLE bonding, strict SSH host trust and CA/SAN-validated TLS. Production identity, durable storage, certificate lifecycle automation, real inference and full AR behavior are outside this Week 7 contract.

**Does this finish the original Week 7 goal?** The latest replacement-iPhone 100-result regression has exact IDs, zero receiver reconnects and zero checked sender counters. The earlier 100- and 6,100-result captures retain a callback discard each, and the original Phone retains its startup retry. Local-forward restoration and receiver restart are verified. Full-run physical foreground confirmation, Phone timing, SSH-master/VPN loss, physical app/screen lifecycle and the teammate's visualizer remain outstanding. The official written course rubric was not available, so this pack makes no grading or rubric-acceptance claim.

**Can you show a restart?** Yes, as a separately labelled fault experiment after a clean baseline, following the owned-process procedure. Successful recovery does not change a fault record into a clean soak.
